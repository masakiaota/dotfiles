interface:
  display_name: "Delegate Worker"
  short_description: "親・subagent・隔離workerへの委任を計画・調整する"
  default_prompt: "Use $delegate-worker to plan and coordinate delegation across the parent, native subagents, and isolated codex exec workers."
